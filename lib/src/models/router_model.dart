class RouterModel {
  RouterModel({
    required this.title,
    required this.process,
    required this.color,
    required this.isArchived,
  });

  final String title;
  final String process;
  final int color;
  final bool isArchived;
}